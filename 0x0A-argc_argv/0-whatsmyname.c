#include <stdio.h>

/**
 * main - main entrance to the program
 * @argc: argument count
 * @argv: argument vector
 * Return: always return zero for success
 **/
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);

	return (0);
}
